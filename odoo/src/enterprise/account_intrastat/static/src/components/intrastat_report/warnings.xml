<?xml version="1.0" encoding="UTF-8" ?>
<templates>
    <t t-name="account_intrastat.intrastat_warning_expired_trans" owl="1">
        Some lines have expired intrastat
        <a
            type="action"
            t-on-click="(ev) => controller.reportAction(ev, 'action_invalid_code_moves', warningParams)"
        >
            transaction codes
        </a>.
    </t>

    <t t-name="account_intrastat.intrastat_warning_premature_trans" owl="1">
        Some lines have premature intrastat
        <a
            type="action"
            t-on-click="(ev) => controller.reportAction(ev, 'action_invalid_code_moves', warningParams)"
        >
            transaction codes
        </a>.
    </t>

    <t t-name="account_intrastat.intrastat_warning_missing_trans" owl="1">
        Some lines have undefined
        <a
            type="action"
            t-on-click="(ev) => controller.reportAction(ev, 'action_invalid_code_moves', warningParams)"
        >
            transaction codes
        </a>.
    </t>

    <t t-name="account_intrastat.intrastat_warning_expired_comm" owl="1">
        Check the expired
        <a
            type="action"
            t-on-click="(ev) => controller.reportAction(ev, 'action_invalid_code_products', warningParams)"
        >
            product commodity codes
        </a>.
    </t>

    <t t-name="account_intrastat.intrastat_warning_premature_comm" owl="1">
        Check the premature
        <a
            type="action"
            t-on-click="(ev) => controller.reportAction(ev, 'action_invalid_code_products', warningParams)"
        >
            product's commodity codes
        </a>.
    </t>

    <t t-name="account_intrastat.intrastat_warning_missing_comm" owl="1">
        Some products have undefined
        <a
            type="action"
            t-on-click="(ev) => controller.reportAction(ev, 'action_invalid_code_products', warningParams)"
        >
            commodity codes
        </a>.
    </t>

    <t t-name="account_intrastat.intrastat_warning_missing_weight" owl="1">
        Some products have undefined
        <a
            type="action"
            t-on-click="(ev) => controller.reportAction(ev, 'action_undefined_weight_products', warningParams)"
        >
            weights
        </a>
        when they are required.
    </t>

    <t t-name="account_intrastat.intrastat_warning_missing_unit" owl="1">
        Some products have undefined
        <a
            type="action"
            t-on-click="(ev) => controller.reportAction(ev, 'action_undefined_units_products', warningParams)"
        >
            supplementary units
        </a>
        when they are required.
    </t>
</templates>

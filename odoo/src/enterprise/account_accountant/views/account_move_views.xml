<?xml version='1.0' encoding='utf-8'?>
<odoo>
    <record id="view_move_line_tree" model="ir.ui.view">
        <field name="name">account.move.line.tree</field>
        <field name="model">account.move.line</field>
        <field name="inherit_id" ref="account.view_move_line_tree"/>
        <field name="mode">extension</field>
        <field name="arch" type="xml">
            <xpath expr="//tree" position="attributes">
                <attribute name="js_class">account_move_line_list</attribute>
            </xpath>
            <xpath expr="//tree" position="inside">
               <header>
                    <button name="action_reconcile" type="object" string="Reconcile" groups="account.group_account_user"/>
                </header>
                <field name="move_attachment_ids" invisible="1">
                    <tree>
                        <field name="mimetype"/> <!--  We want to fetch the data for using it in the js-->
                    </tree>
                </field>
                <field name="is_account_reconcile" invisible="1"/>
                <field name="has_deferred_moves" invisible="1"/>
            </xpath>
            <xpath expr="//tree/field[@name='name']" position="after">
                <field name="deferred_start_date" optional="hide" attrs="{'readonly': [('has_deferred_moves', '=', True)]}"/>
                <field name="deferred_end_date" optional="hide" attrs="{'readonly': [('has_deferred_moves', '=', True)]}"/>
            </xpath>
            <xpath expr="//tree/field[@name='matching_number']" position="attributes">
                <attribute name="widget">matching_link_widget</attribute>
            </xpath>
        </field>
    </record>

    <record id="view_move_form_inherit" model="ir.ui.view">
        <field name="name">account.move.form.inherit</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_move_form"/>
        <field name="arch" type="xml">
            <xpath expr="//page[@id='other_tab']//field[@name='fiscal_position_id']" position="after">
                <field name="inalterable_hash" groups="base.group_no_one"/>
            </xpath>
            <xpath expr="//page[@id='other_tab_entry']//field[@name='company_id']" position="after">
                <field name="inalterable_hash" groups="base.group_no_one"/>
            </xpath>
            <xpath expr="//tree/field[@name='analytic_distribution']" position="after">
                <field name="deferred_start_date" optional="hide"/>
                <field name="deferred_end_date" optional="hide"/>
            </xpath>
            <xpath expr="//div[@name='button_box']" position="inside">
                <button name="action_open_bank_reconciliation_widget_statement"
                        class="oe_stat_button"
                        icon="fa-bars"
                        type="object"
                        attrs="{'invisible': ['|', ('statement_id', '=', False), ('state', '=', 'cancel')]}">
                        <div class="o_stat_info">
                            <span class="o_stat_text">Bank Statement</span>
                        </div>
                </button>
                <button name="action_open_bank_reconciliation_widget"
                        class="oe_stat_button"
                        icon="fa-bars"
                        type="object"
                        attrs="{'invisible': ['|', '|', '|', ('move_type', '!=', 'entry'), ('id', '=', False), ('statement_line_id', '=', False), ('state', '=', 'cancel')]}">
                        <div class="o_stat_info">
                            <span class="o_stat_text">1 Bank Transaction</span>
                        </div>
                </button>
                <field name="deferred_move_ids" invisible="1"/>
                <field name="deferred_original_move_ids" invisible="1"/>
                <field name="deferred_entry_type" invisible="1"/>
                <button name="open_deferred_entries"
                        class="oe_stat_button"
                        icon="fa-bars"
                        type="object"
                        attrs="{'invisible': [('deferred_move_ids', '=', [])]}"
                        string="Deferred Entries">
                </button>
                <button name="open_deferred_original_entry"
                        class="oe_stat_button"
                        icon="fa-bars"
                        type="object"
                        attrs="{'invisible': ['|', ('deferred_entry_type', '=', 'revenue'), ('deferred_original_move_ids', '=', [])]}"
                        string="Related Purchase">
                </button>
                <button name="open_deferred_original_entry"
                        class="oe_stat_button"
                        icon="fa-bars"
                        type="object"
                        attrs="{'invisible': ['|', ('deferred_entry_type', '=', 'expense'), ('deferred_original_move_ids', '=', [])]}"
                        string="Related Sale">
                </button>
            </xpath>
        </field>
    </record>

    <record id="action_move_line_posted_unreconciled" model="ir.actions.act_window">
        <field name="name">Journal Items to Reconcile</field>
        <field name="res_model">account.move.line</field>
        <field name="view_mode">tree,pivot,graph,kanban</field>
        <field name="view_id" ref="view_move_line_tree"/>
        <field name="domain">[('display_type', 'not in', ('line_section', 'line_note'))]</field>
        <field name="context">{'journal_type':'general', 'search_default_posted':1, 'search_default_unreconciled':1}</field>
    </record>

    <record id="action_open_auto_reconcile_wizard" model="ir.actions.act_window">
        <field name="name">Find Entries to Reconcile Automatically</field>
        <field name="res_model">account.auto.reconcile.wizard</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
    </record>
</odoo>

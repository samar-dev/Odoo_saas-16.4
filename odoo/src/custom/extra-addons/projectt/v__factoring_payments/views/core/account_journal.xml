<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>

        <record id="view_account_journal_form_inherit" model="ir.ui.view">
            <field name="name">view_account_journal_form_inherit</field>
            <field name="model">account.journal</field>
            <field name="inherit_id" ref="account.view_account_journal_form" />
            <field name="arch" type="xml">

                <xpath expr="//notebook" position="inside">
                    <page string="Factoring Commissions" attrs="{'invisible': [('x_type', '!=', 'factoring')]}">
                        <field name="journal_factoring_line_ids">
                            <tree editable="bottom">
                                <field name="company_id" invisible="1" />
                                <field name="sequence" widget="handle" />
                                <field name="name" />
                                <field name="label"
                                       placeholder="Label to appear in the journal item" />
                                <field name="value_type" />
                                <field name="value" />
                                <field name="account_id"
                                       options="{'no_create': 1, 'no_edit': 1}" />
                            </tree>
                        </field>
                    </page>
                </xpath>

            </field>
        </record>

    </data>
</odoo>

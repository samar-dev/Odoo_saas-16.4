<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>

        <record id="data_access_guard_view_form" model="ir.ui.view">
            <field name="name">data_access_guard_view_form</field>
            <field name="model">data.access.guard</field>
            <field name="arch" type="xml">
                <form string="data_access_guard_form">
                    <sheet>
                        <group>
                            <field name="model_id"
                                   options="{'no_create': 1, 'no_edit': 1}" />
                            <field name="group_ids"
                                   options="{'no_create': 1, 'no_edit': 1}"
                                   widget="many2many_tags" />
                            <field name="user_ids"
                                   options="{'no_create': 1, 'no_edit': 1}"
                                   widget="many2many_tags" />
                        </group>
                        <notebook>

                            <page string="Python Code"
                                  name='filter_domain_page'
                                  autofocus="autofocus">
                                <field name="filter_domain"
                                       widget="ace"
                                       options="{'mode': 'python'}"
                                       placeholder="Enter Python code here. Help about Python expression is available in the help tab of this document." />
                            </page>
                        </notebook>
                    </sheet>
                </form>
            </field>
        </record>

        <record id="data_access_guard_view_tree" model="ir.ui.view">
            <field name="name">data_access_guard_view_tree</field>
            <field name="model">data.access.guard</field>
            <field name="arch" type="xml">
                <tree string="data_access_guard_tree">
                    <field name="model_name" invisible="1" />
                    <field name="model_id" options="{'no_create': 1, 'no_edit': 1}" />
                    <field name="filter_domain" />
                    <field name="group_ids"
                           options="{'no_create': 1, 'no_edit': 1}"
                           widget="many2many_tags" />
                    <field name="user_ids"
                           options="{'no_create': 1, 'no_edit': 1}"
                           widget="many2many_tags" />
                    <field name="create_uid" optional="hide" />
                    <field name="create_date" optional="hide" />
                    <field name="write_date" optional="hide" />
                </tree>
            </field>
        </record>


        <record id="data_access_guard_action" model="ir.actions.act_window">
            <field name="name">Data Access Guard</field>
            <field name="type">ir.actions.act_window</field>
            <field name="res_model">data.access.guard</field>
            <field name="view_mode">tree,form</field>
        </record>

    </data>
</odoo>
